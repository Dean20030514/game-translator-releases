refactor(round-64): r63 audit path X first wave — close 11 findings + fix 3 audit-tail regressions

User selected path X (all 23 r63 audit findings to fix, ~r64-r65 two rounds).
Round 64 closes dimensions 1+2+3 (Tech / Quality+Security / Architecture+Design)
= 11 findings: T1-T4 + S1-S4 + A1-A3, plus 3 pre-existing silent regressions
surfaced as audit-tail of S1.

== T1 (HIGH) — Pre-emptive split of 3 testfiles near 800-line cap ==

Each was 2-10 lines from blocking new test additions:

  tests/test_file_safety.py        798 → 151 (helper unit tests)
                                       + 692 (new test_file_safety_loaders.py
                                              with 16 caller-integration TOCTOU
                                              regressions)

  tests/test_api_client.py         792 → 656 (config + helpers + HTTP/API tests)
                                       + 173 (new test_api_client_response_cap.py
                                              with 5 response-body cap tests)

  tests/test_verify_docs_claims.py 790 → 553 (16 helper unit tests)
                                       + 405 (new test_verify_docs_claims_main.py
                                              with 12 main_fast_path integration
                                              tests; _make_fixture_repo helper
                                              copied since both files need it)

All 21+27+28 = 76 tests preserved (now distributed across 6 files).

== S1 (HIGH) — Rewrite meta-runner as subprocess-discover-and-run ==

tests/test_all.py changed from hand-curated 11-file import list (191 tests
0.68s) to glob-discovery subprocess execution of all tests/test_*.py
(37 files, ~371 verified tests via banner parse, ~7s wall-clock). New tests
added in future rounds will be auto-included; the 24-file silent feedback
gap that existed for 6+ rounds is closed.

This change immediately surfaced 3 PRE-EXISTING silent regressions hiding
behind the 39% pre-commit coverage:

(a) tests/test_batch1.py multi-language tests stale since r52 C4 BREAKING.
    Tests asserted strings like config.default_language = "japanese" but
    pipeline.stages._generate_default_language has been hardcoded to
    "chinese" since r52. Renamed tests with _kwarg_ignored_post_r52 suffix
    and updated assertion to "chinese".

(b) tests/test_rpyc_decompiler.py imports referenced 5/8 identifiers that
    no longer exist in tools.rpyc_decompiler (RPYC2_HEADER,
    _RestrictedUnpickler, _read_rpyc_data, _safe_unpickle,
    extract_strings_from_rpyc). File deleted as 582 lines of dead test
    debt; r65+ may rebuild against current API if needed.

(c) tests/test_single.py is a manual integration script with input()
    prompt for API key, not a unit test. Added to test_all.py
    _NON_TEST_FILES exclusion. File preserved as manual script.

== S2 (MEDIUM) — AUDIT.md permanent entry point ==

Pre-r64 AUDIT_R57.md filename had drifted to host the r60 cycle (closed)
and now r63 cycle. Renamed via git mv to _archive/AUDIT_R63.md and created
new AUDIT.md as the permanent active-cycle entry point.

CLAUDE.md docs index, HANDOFF, docs/ARCHITECTURE, 2 workflow files, and
ADR 0011 references all updated to AUDIT.md (active) or _archive/AUDIT_R63.md
(historical r63 cycle reference).

== S3 (MEDIUM) — START.bat fix ==

Pre-r64: '[ERROR] Python 3.9+ is required.' contradicted r57 T1 BREAKING
(Python 3.10 floor). Updated message to 3.10+ and added explicit version
check via python -c sys.version_info >= (3, 10) that fails fast with a
readable message and ADR 0006 reference.

== S4 (LOW) — main.py --version flag ==

argparse action="version" reads pyproject.toml::version via small regex
helper _read_project_version() (avoids tomllib import which is 3.11+).
Verified: python main.py --version outputs 'main.py 2.0.0'.

== T2-T4 + A1-A3 — Documentation batch ==

Six items added to CLAUDE.md known limitations section as watchlist or
architectural-decision retires:

  T2 — 4+ production files near 800-line cap watchlist
  T3 — Hint coverage measurement clarification: production-only is 87.1%
       (the 43.2% cited in r60 audit T2 was inflated by including tests/)
  T4 — .pyc residue retire to architectural decision
  A1 — gui.py 594-line watchlist persisted
  A2 — pipeline/stages.py function-internal imports must docstring lazy reasoning
  A3 — Empty __init__.py inconsistency retire to architectural decision

== Numbers ==
tests_total:      498 -> 480 (-18, deletion of test_rpyc_decompiler 18 tests)
test_files:       36 -> 38  (+2 net: +3 split files, -1 deleted)
ci_steps:         36 unchanged
assertion_points: 624 -> 606 (-18, mirrors tests_total)

== Hard contracts ==
Still 15. r64 closures are documentation, splits, fixes, and retires;
no new contracts.

24th consecutive 0-CRITICAL streak (r35-r64).
